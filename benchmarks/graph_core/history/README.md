# Historical entrypoint source

`confirm_graph_core_v1.py` is the exact former public CLI source, preserved as a
byte-identical provenance artifact. It is not executable from this relocated
position. The live `benchmarks/confirm_graph_core.py --protocol unbounded-v1`
retains the original route; `compact-v2` is a separate nine-arm mapping followup.
No historical result or numerical kernel is changed by this source archive.
